# AI Customization Instructions

이 저장소를 사용자가 "내 미니홈피 링크트리로 바꿔줘"라고 주면 바로 코드를 고치기 전에 아래 질문을 먼저 하세요. 답을 받으면 `src/config/linktree.ts`, `src/config/theme.ts`, (연재물을 쓴다면) `src/config/miyotoon.ts`를 수정하세요.

## 먼저 물어볼 질문

**필수**

1. 표시 이름은 무엇인가요? 예: DoRms 교사 OOO
2. 첫 화면 소개 문구에 들어갈 한 줄 설명은 무엇인가요?
3. 왼쪽 아래 파도타기 목록 첫 번째 항목에 들어갈 `도름스 커뮤니티 나의 활동` 링크 주소(URL)는 무엇인가요?
4. 색감은 어떤 쪽이 좋나요? 따뜻한 크림, 차분한 파스텔, 밝은 교실톤, 진한 대비 중 하나로 물어보세요.

**선택 (필요 없으면 건너뛰고 기본값/빈 상태로 두세요)**

5. 자기소개 섹션에 넣을 소개 글, 하고 있는 일들 목록, 연락처가 있나요? (`profileSections`)
6. 게시판 탭에 올릴 앱이나 글 링크가 있나요? 제목, 한 줄 설명, 날짜, URL, 미리보기 이미지를 받으세요. (`boardPosts`)
7. 사진첩에 넣을 사진이 있나요? 사진 파일은 미리 `public/assets/`에 넣어달라고 하세요. (`photos`)
8. 왼쪽 아래 파도타기 목록에 (도름스 활동 링크 외에) 추가로 넣을 자주 가는 링크가 있나요? (`waveLinks`)
9. 미니홈피 배경음악을 쓸까요? 쓴다면 유튜브 영상 링크(재생목록이 이어붙은 한 영상이어도 됨)와 각 곡 시작 지점을 받으세요. 안 쓰면 `bgmTracks`를 빈 배열로 둡니다. (`bgmTracks`)
10. 방명록에 미리 넣어둘 예시 글이 있나요? 없으면 기본 예시 글을 그대로 두거나 비워도 됩니다. (`guestbook`)
11. 방문자가 실제로 방명록을 남기고 방문 수가 실제로 집계되길 원하나요? 원한다면 Firebase(Firestore) 프로젝트를 직접 만들어 `.env.local`에 `NEXT_PUBLIC_FIREBASE_*` 값을 채우고 `firestore.rules`를 배포해야 한다고 안내하세요. AI가 대신 Firebase 프로젝트를 만들거나 키를 발급할 수는 없습니다.

연재물(미요툰) 탭은 질문하지 마세요. `src/config/miyotoon.ts`의 `episodes`는 원 저작자의 개인 만화라서, 다른 사람의 링크트리를 만들 때는 기본적으로 비워 두세요(그러면 "미요툰" 탭이 자동으로 숨겨집니다). 사용자가 먼저 "나도 연재물을 넣고 싶다"고 말하면 그때만 회차 수와 이미지 경로를 물어보고 채우세요.

## 고정 규칙

- 왼쪽 아래 파도타기 목록의 첫 번째 항목은 반드시 `도름스 커뮤니티 나의 활동`입니다. 지우거나 순서를 바꾸지 마세요.
- 홈 화면 인트로의 `모든 활동 구경하기` 버튼은 그대로 둡니다. 메인 링크 카드 목록(여러 링크를 모아 보여주는 기능)은 이 템플릿에 없습니다. 다시 만들지 마세요.
- DoRms 팀 제품별 이미지나 서비스 내부 이미지는 추가하지 않습니다.
- DoRms 본 서비스의 코드, API, 인증, 환경변수, 운영 URL, 비공개 자산을 가져오지 않습니다.
- 사용자가 준 링크가 비공개이거나 개인정보가 들어간 URL이면 공개 저장소에 올려도 되는지 다시 확인합니다.
- 화면 문구는 쉬운 한국어로 씁니다. 코드와 식별자는 영어로 둡니다.
- 긴 줄표 문자는 쓰지 않습니다.
- 연재물, BGM, 사진첩은 모두 선택 기능입니다. 사용자가 원하지 않으면 억지로 채우지 말고 빈 배열로 두세요. 파도타기는 첫 항목(도름스 활동 링크)만 고정이고, 그 외 추가 항목은 선택입니다.

## 수정 위치

- 프로필, 자기소개, 게시판, 사진첩, 파도타기, BGM, 방명록: `src/config/linktree.ts`
- 연재물(웹툰형) 회차: `src/config/miyotoon.ts`
- 색상: `src/config/theme.ts`
- 공유 미리보기 이미지(카카오톡·인스타 등에 링크 공유 시 뜨는 썸네일): `public/assets/dorms-community.png` (`src/app/layout.tsx`의 `openGraph.images`)
- 방명록·방문 수를 실제로 저장하려면: `.env.local`의 `NEXT_PUBLIC_FIREBASE_*`, `firestore.rules`

## 검증

수정 후 아래를 실행하세요.

```bash
npm run build
```

가능하면 데스크톱과 모바일 폭에서 다음을 확인하세요.

- 첫 진입에서 스파이럴 화면이 보이는지
- `모든 활동 구경하기`를 누르면 홈 화면(미니룸, 방명록)으로 넘어가는지
- 왼쪽 아래 파도타기 드롭다운 첫 번째 항목이 `도름스 커뮤니티 나의 활동`인지
- 선택 기능(연재물, BGM, 사진첩)을 켰다면 해당 탭/영역이 제대로 보이는지, 안 썼다면 빈 상태로 자연스럽게 숨겨지는지
